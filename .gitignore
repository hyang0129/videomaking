# OS / editor
.DS_Store
Thumbs.db
Desktop.ini
.vscode/

# Video / audio / image exports (usually large)
exports/
render/
renders/
out/
output/

# Raw footage (do not commit)
raw/
footage/
recordings/

# Common large binaries (Videos)
*.mp4
*.mov
*.mkv
*.avi

# Audio (usually okay if small, but ignore large raw wavs)
raw_audio/
*.wav
*.aif
*.aiff
*.flac

# Visual Assets (Allow specific directories for slides/charts)
# We ignore global exports but allow assets/ and projects/ images
*.png
*.jpg
*.jpeg
!assets/**/*.png
!assets/**/*.jpg
!projects/**/*.png
!projects/**/*.jpg

# Project/cache/temp files (extend as needed)
*.tmp
*.temp
*.bak
.cache/
